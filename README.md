# WindTone AI

Интеллектуальный AI-ассистент с поддержкой голосовых сообщений, транскрибации аудио/видео файлов и обработки текстовых документов.

## 🚀 Возможности

- **Умный чат с AI** - общение с AI на базе OpenAI GPT-3.5-turbo
- **Голосовые сообщения** - запись аудио и получение ответов в виде голосовых сообщений
- **Транскрибация файлов** - автоматическая транскрибация аудио и видео файлов через OpenAI Whisper
- **Обработка документов** - чтение и анализ текстовых файлов (txt, md, code files)
- **Выбор голоса** - 6 различных голосов для озвучки ответов AI
- **Современный UI** - адаптивный дизайн с поддержкой мобильных устройств
- **История чата** - сохранение истории сообщений в localStorage

## 🛠 Технологии

- **Next.js 16** - React фреймворк для production
- **React 19** - библиотека для построения пользовательских интерфейсов
- **TypeScript** - типизированный JavaScript
- **Tailwind CSS** - utility-first CSS фреймворк
- **Zustand** - легковесное управление состоянием
- **OpenAI API** - интеграция с GPT и Whisper
- **React Icons** - библиотека иконок

## 📋 Требования

- Node.js 18+ 
- Yarn
- OpenAI API ключ

## 🔧 Установка

### 1. Клонирование репозитория

```bash
git clone <repository-url>
cd hackathon-client
```

### 2. Установка зависимостей

```bash
yarn install
```

### 3. Настройка переменных окружения

Создайте файл `.env.local` в корне проекта:

```bash
OPENAI_API_KEY=your_openai_api_key_here
```

**Где получить API ключ:**
1. Перейдите на [OpenAI Platform](https://platform.openai.com/)
2. Зарегистрируйтесь или войдите в аккаунт
3. Перейдите в раздел [API Keys](https://platform.openai.com/api-keys)
4. Создайте новый API ключ
5. Скопируйте ключ и вставьте в `.env.local`

⚠️ **Важно:** Никогда не коммитьте файл `.env.local` в репозиторий!

### 4. Запуск проекта

Запуск development сервера:

```bash
yarn dev
```

Откройте [http://localhost:3000](http://localhost:3000) в браузере.

## 📁 Структура проекта

```
hackathon-client/
├── src/
│   ├── app/                    # Next.js App Router
│   │   ├── api/               # API routes
│   │   │   ├── openai/        # OpenAI chat completions
│   │   │   ├── tts/           # Text-to-Speech API
│   │   │   └── transcribe/    # Audio transcription
│   │   ├── chat/              # Страница чата
│   │   ├── about/             # Страница "О нас"
│   │   └── page.tsx           # Главная страница
│   ├── components/            # React компоненты
│   │   ├── AudioMessage/      # Компонент аудио сообщений
│   │   ├── ChatInput/         # Компонент ввода сообщений
│   │   ├── ChatMessages/      # Компонент отображения сообщений
│   │   ├── ExtraOptions/      # Дополнительные опции (файлы, озвучка)
│   │   └── Header/            # Компонент шапки сайта
│   ├── features/              # Бизнес-логика
│   │   └── data.ts            # Работа с localStorage и API
│   ├── store/                 # State management
│   │   └── useChatStore.ts    # Zustand store для чата
│   └── types/                 # TypeScript типы
├── .env.local                 # Переменные окружения (создать вручную)
├── package.json
├── tailwind.config.js
└── tsconfig.json
```

## 🎯 Использование

### Чат с AI

1. Перейдите на страницу `/chat`
2. Введите сообщение в поле ввода
3. Нажмите кнопку отправки или Enter
4. Получите ответ от AI

### Отправка файлов

1. Нажмите кнопку "+" рядом с полем ввода
2. Выберите файл (текстовый, аудио или видео)
3. Файл будет автоматически обработан:
   - **Аудио/Видео** - транскрибировано через Whisper
   - **Текстовые файлы** - прочитано содержимое
4. AI получит содержимое файла и сможет ответить на вопросы о нем

### Голосовые сообщения

1. Нажмите кнопку с иконкой волны для записи аудио
2. Запишите сообщение
3. Остановите запись
4. Аудио будет автоматически добавлено к сообщению

### Озвучка ответов AI

1. Перед отправкой сообщения нажмите кнопку "+"
2. Включите опцию "Включить озвучку"
3. Отправьте сообщение
4. Ответ AI будет озвучен и отображен как аудио сообщение
5. Можно выбрать голос для озвучки через настройки (шестеренка)

### Очистка чата

1. На странице чата нажмите кнопку "Очистить чат"
2. Подтвердите действие
3. История сообщений будет удалена

## 🏗 Сборка для production

```bash
yarn build
```

Запуск production сервера:

```bash
yarn start
```

## 📝 API Routes

### `/api/openai`
- **Метод:** POST
- **Описание:** Отправка сообщений в OpenAI Chat API
- **Параметры:** 
  - `messages` (JSON string) - массив сообщений
  - `files` (File[]) - файлы для обработки

### `/api/tts`
- **Метод:** POST
- **Описание:** Генерация аудио из текста через OpenAI TTS
- **Параметры:**
  - `text` (string) - текст для озвучки
  - `voice` (string) - выбор голоса (alloy, echo, fable, onyx, nova, shimmer)

### `/api/transcribe`
- **Метод:** POST
- **Описание:** Транскрибация аудио/видео файлов
- **Параметры:**
  - `file` (File) - аудио или видео файл

## 🔒 Безопасность

- API ключ OpenAI хранится только на сервере в переменных окружения
- Никогда не передавайте API ключ на клиент
- Используйте `.env.local` для локальной разработки
- Для production используйте переменные окружения вашего хостинга

## 🐛 Решение проблем

### Ошибка "OpenAI API key is not configured"
- Убедитесь, что файл `.env.local` создан в корне проекта
- Проверьте, что переменная `OPENAI_API_KEY` указана правильно
- Перезапустите development сервер после создания `.env.local`

### Аудио не воспроизводится
- Проверьте, что браузер поддерживает Web Speech API
- Убедитесь, что микрофон доступен для записи
- Проверьте разрешения браузера для доступа к микрофону

### Файлы не обрабатываются
- Убедитесь, что файл не превышает лимиты OpenAI API
- Проверьте формат файла (поддерживаются: аудио, видео, текстовые файлы)
- Проверьте консоль браузера на наличие ошибок

## 📄 Лицензия

Этот проект создан для хакатона.

## 👥 Авторы

**Павел Пичугин** - Глава и один из основателей команды <QCoders/>, FullStack Developer

## 🛠 Стек разработки

- **Next.js** - React фреймворк
- **Yarn** - менеджер пакетов
- **TypeScript** - типизированный JavaScript
- **Zustand** - управление состоянием
- **Tailwind CSS** - utility-first CSS фреймворк

---

**Приятного использования! 🚀**
